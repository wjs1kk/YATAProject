<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.itwillbs.yata.mapper.MemberMapper">
  	<select id="selectUser" resultType="com.itwillbs.yata.vo.MemberVO">
		select * 
			from member
			where 
				member_email = #{member_email} 
	</select>
	<insert id="insertUser">
		insert into member (
			member_email, member_passwd, 
			member_name, member_date, 
			member_gender, member_isadmin, member_point,member_license
			) 
			value(
			#{member_email}, #{member_passwd},
			#{member_name}, now(),
			#{member_gender}, 0, 2000,1)
	</insert>
	<select id="selectPasswd" resultType="string">
		SELECT member_passwd
		  FROM member
		 WHERE member_email = #{member_email}
	</select>
	<!-- 회원수정 -->
	<update id="modifyUser">
		UPDATE member			
			SET member_passwd = #{member_passwd}
			WHERE member_email = #{member_email}
	</update>
	
	<!-- 회원탈퇴 -->
	<delete id="deleteUser">
		DELETE
			FROM member
				WHERE member_email = #{member_email}
	</delete>
	<!-- 	쿠폰 등록시 사용자 존재 확인 -->
	<select id="searchMemberEmail" resultType="String">
		SELECT *
		  FROM member
		 WHERE member_email = #{member_email}
	</select>
	
	
	
	<update id="AdminMemberModify">
		UPDATE member
			SET member_name = #{member_name}
				<if test='member_passwd neq ""'>
			    , member_passwd = #{member_passwd}
				</if>
			    , member_point = #{member_point}
			    <if test='member_license neq ""'>
			    , member_license = #{member_license}
				</if>
			WHERE member_email = #{member_email}
	</update>

	<delete id="AdminMemberDelete">
		DELETE FROM member
		WHERE member_email = #{member_email}
	</delete>
	<select id="memberList" resultType="com.itwillbs.yata.vo.MemberVO">
		SELECT *
		  FROM member
		<if test="!searchType.equals('')">
			WHERE
			<choose>
				<when test="searchType.equals('MEMBER_EMAIL')">
					MEMBER_EMAIL LIKE CONCAT('%', #{searchKeyword}, '%')
				</when>
				<when test="searchType.equals('MEMBER_PHONE')">
					MEMBER_PHONE LIKE CONCAT('%', #{searchKeyword}, '%')
				</when>
				<when test="searchType.equals('MEMBER_DATE')">
					MEMBER_DATE LIKE CONCAT('%', #{searchKeyword}, '%')
				</when>
			</choose>
		</if>
		 ORDER BY member_date DESC
		 LIMIT #{startRow}, #{listLimit}
	</select>
	<select id="selectMemberListCount" resultType="int">
		SELECT COUNT(member_email)
		FROM member
		<if test="!searchType.equals('')">
			WHERE
			<choose>
				<when test="searchType.equals('MEMBER_EMAIL')">
					MEMBER_EMAIL LIKE CONCAT('%', #{searchKeyword}, '%')
				</when>
				<when test="searchType.equals('MEMBER_PHONE')">
					MEMBER_PHONE LIKE CONCAT('%', #{searchKeyword}, '%')
				</when>
				<when test="searchType.equals('MEMBER_DATE')">
					MEMBER_DATE LIKE CONCAT('%', #{searchKeyword}, '%')
				</when>
			</choose>
		</if>
	</select>
	<select id="isAdmin" resultType="String">
		SELECT member_isadmin
		FROM member
		WHERE member_email = #{member_email}
	</select>
	<!-- 2023-04-27 김동욱 - 면허증 등록 신청 목록 -->
	<select id="selectLicenseList" resultType="map">
		SELECT l.member_email as member_email, license_num, license_pass
		FROM license l JOIN member m
		ON l.member_email = m.member_email
	    WHERE MEMBER_LICENSE = '0';
	</select>
	
	<!--  2023-04-27 김동욱 - 관리자페이지에서 면허증 승인 후 맴버 테이블 member_licence 1로 업데이트 -->
	<update id="updateMemberLicense">
		UPDATE member SET member_license = '1'
		WHERE member_email = #{lic_member_email}
	</update>
	<select id="loginLookup" resultType="String">
		SELECT member_email
		FROM member
		WHERE member_email = #{member_email}
	</select>
	<!-- 2023-04-27 김동욱 - 회원가입 이메일 중복 체크 -->
	<select id="selectEmailCheck" resultType="String">
		SELECT member_email
		FROM member
		WHERE member_email = #{member_email}
	</select>
	<!-- 면허인증 -->
	<insert id="insertLicense" parameterType="string">
		INSERT INTO
			license
				VALUES(
					null,
					#{member_email},
					#{license_num},
					#{license_pass}
				)
	</insert>
  </mapper>